# 2026-05-08

## Goal

Clarify the remaining Hermes collaboration autonomy gap after the read-only collaboration/search milestone and update the roadmap before implementing a worker.

## Work completed

- Documented that collaboration requests addressed to `hermes-agent` are visible to Hermes but do not currently invoke Hermes runtime.
- Updated `docs/collaboration-pm-agent-plan.md` with delivered capabilities, the active inbound execution gap, and a future inbound request bridge phase.
- Created `docs/roadmap.md` with the current documentation milestone and future bridge/writeback milestones.
- Created `docs/findings.md` to record that collaboration visibility does not imply Hermes execution.
- Created `docs/backlog.md` with bridge contract, invocation, deduplication, safety, observability, and acceptance-test items.
- Created `docs/lessons.md` as the project common lessons document placeholder.

## Validation

- Documentation-only change; no runtime worker, webhook, scheduler, or write-capable collaboration tool was implemented.
- Roadmap keeps a single current marker for the documentation/gap update milestone.

## Follow-up implementation check

- Reviewed the pending collaboration monitor and cron job changes.
- Confirmed `inbound_requests` monitor support, deduplicated seen-state handling, and `create_collaboration_monitor` job creation path.
- Fixed the cronjob tool schema description so `monitor_kind` documents `inbound_requests` as a supported value.
- Isolated the cron scheduler test lock path per test to prevent xdist workers from skipping `tick()` assertions because of a shared runtime lock.

## Follow-up validation

- `python -m pytest tests/tools/test_collaboration_monitor.py tests/tools/test_cronjob_tools.py -q` → 55 passed.
- `python -m pytest tests/test_quick_commands.py tests/cron/test_scheduler.py tests/cron/test_jobs.py tests/hermes_cli/test_cron.py tests/tools/test_cron_prompt_injection.py tests/tools/test_collaboration_monitor.py tests/tools/test_cronjob_tools.py -q` → 180 passed.

## Simplify cleanup

- Centralized collaboration monitor kind aliases, valid kind checks, and default schedule mappings in `tools/collaboration_monitor.py`.
- Updated CLI, gateway, and cronjob creation paths to use the shared monitor normalization and schedule constants.
- Replaced inbound monitor state writes with `atomic_json_write` and removed duplicate request identity computation.
- Avoided rewriting inbound seen-state when no request metadata changed.

## Simplify validation

- `python -m pytest tests/tools/test_collaboration_monitor.py tests/tools/test_cronjob_tools.py -q` → 55 passed.
- `python -m pytest tests/test_quick_commands.py tests/cron/test_scheduler.py tests/cron/test_jobs.py tests/hermes_cli/test_cron.py tests/tools/test_cron_prompt_injection.py tests/tools/test_collaboration_monitor.py tests/tools/test_cronjob_tools.py -q -n 0` → 180 passed, 2 existing async mock warnings.
- Fixed `tests/test_quick_commands.py::TestGatewayQuickCommands::test_timeout_returns_error` so it mocks subprocess creation instead of launching a real `sleep 100` process under xdist.
- `python -m pytest tests/test_quick_commands.py::TestGatewayQuickCommands::test_timeout_returns_error -q` → 1 passed.
- `python -m pytest tests/test_quick_commands.py tests/cron/test_scheduler.py tests/cron/test_jobs.py tests/hermes_cli/test_cron.py tests/tools/test_cron_prompt_injection.py tests/tools/test_collaboration_monitor.py tests/tools/test_cronjob_tools.py -q` → 180 passed.

## Local inbox bridge milestone

- Added read-only local inbox persistence for inbound collaboration requests addressed to `hermes-agent`.
- Inbound monitor runs now keep seen-state deduplication and upsert open requests into a Hermes-local pending inbox.
- Added `/collab inbox` for CLI and guarded gateway review of pending local inbound requests.
- Kept preview runs side-effect free and kept collaboration writeback/runtime invocation out of scope.

## Local inbox validation

- `python -m pytest tests/tools/test_collaboration_monitor.py tests/test_quick_commands.py tests/tools/test_cronjob_tools.py -q` → 84 passed, with existing pytest unawaited AsyncMock warnings.

## Approval-gated response flow

- Added a local collaboration response outbox for draft/list/show/post lifecycle under Hermes home.
- Wired `/collab respond draft|list|show|post` in the CLI with exact action-ID confirmation before any network write.
- Kept `tools/collaboration_tool.py` read-only and blocked `/collab respond` posting from gateway execution.
- Updated command metadata and roadmap so approval-gated response posting is complete and limited automation evaluation is the current item.

## Approval-gated response validation

- `python -m pytest tests/tools/test_collaboration_responses.py -q` → 8 passed.
- `python -m pytest tests/tools/test_collaboration_monitor.py tests/test_quick_commands.py -q` → 47 passed.
- Final verification reproduced no AsyncMock RuntimeWarning.

## Live approval-gated response smoke test

- Created test collaboration request `REQ-20260508-007` for the Hermes approval-gated response flow.
- Confirmed local draft creation occurred before any collaboration write.
- Initial approved POST returned HTTP 422 because the Board `respond` endpoint requires `assignee`.
- Updated Hermes response POST payload to include a configurable assignee defaulting to `hermes-agent`.
- Re-ran the live draft/post flow successfully; request moved to `delivered` and was then closed as `completed`.

## Live smoke validation

- `python -m pytest tests/tools/test_collaboration_responses.py tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_draft_prints_action_id_and_body tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_list_prints_drafts tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_post_denies_wrong_action_id tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_post_accepts_exact_action_id -q` → 12 passed.
- Live request `REQ-20260508-007` produced response entry `msg-002` with `assignee: hermes-agent`, `transition: open -> delivered`.

## End-to-end smoke coverage

- Added local smoke coverage for “request to hermes-agent → surfaced pending item → approved response”.
- The test combines inbound monitor persistence, `/collab inbox`-equivalent pending read, local response draft creation, and mocked approved Board response posting.
- Updated roadmap smoke coverage item to complete.

## Smoke coverage validation

- `python -m pytest tests/tools/test_collaboration_monitor.py tests/tools/test_collaboration_responses.py tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_draft_prints_action_id_and_body tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_list_prints_drafts tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_post_denies_wrong_action_id tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_post_accepts_exact_action_id -q` → 29 passed.

## Collaboration write-default verification

- Confirmed the general collaboration tool remains read-only and rejects write-shaped actions.
- Confirmed default collaboration config keeps `read_only: true` and monitoring disabled unless explicitly enabled.
- Confirmed the only write path exercised here is the CLI approval-gated response path with exact action-ID confirmation.
- Updated roadmap write-default validation item to complete.

## Runtime deployment expectations

- Updated the collaboration PM plan to mark approval-gated response writes as partially delivered.
- Documented always-on runtime constraints for read-only-first monitoring, local binding, secret handling, gateway cron installation, scheduler idempotency, Hermes home persistence, service documentation, shared agent registry registration, and repeated manual response validation before write automation.
- Updated roadmap runtime deployment documentation item to complete.

## Repeated live approval-gated response smoke test

- Created second live smoke request `REQ-20260508-008` for repeated approval-gated response operation.
- Confirmed local draft creation occurred before any collaboration write.
- Posted the response through the approval-gated path successfully with action id `collab-act-20260508T060225-b6afd1e5c6`.
- Verified the request produced response entry `msg-002` with `assignee: hermes-agent`, `transition: open -> delivered`.
- Closed the repeated smoke request as `completed`.

## Repeated live smoke validation

- Live request `REQ-20260508-008` reached `delivered` and was closed as `completed`.
- Two live approval-gated response operations have now succeeded after the Board response payload fix.
- `source venv/bin/activate && python -m pytest -o addopts='' tests/tools/test_collaboration_monitor.py tests/tools/test_collaboration_responses.py tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_draft_prints_action_id_and_body tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_list_prints_drafts tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_post_denies_wrong_action_id tests/test_quick_commands.py::TestCLIQuickCommands::test_collab_respond_post_accepts_exact_action_id -q` → 29 passed.

## Autonomous PM operating criteria manual

- Created `docs/autonomous-pm-operating-criteria.md` as the operating manual for judging HITL to autonomous PM loop readiness.
- Defined required operational logs, KPI definitions, strict initial thresholds, pass/conditional-pass/fail/hard-fail categories, and automation boundaries.
- Set the strict initial readiness target to at least 14 calendar days, at least 10 live HITL handled requests, 0 unapproved writes, 0 critical errors, and 0 forbidden-scope automation attempts.
- Updated the collaboration PM plan to link the readiness criteria and clarify that passing the gate only permits a later limited-autonomy design review.
- Updated the roadmap so the current item is collecting HITL KPI evidence and running the strict pass/fail readiness evaluation.

## Next

- Collect HITL operation evidence against the operating criteria before designing any autonomous PM loop.
