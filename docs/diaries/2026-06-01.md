# 2026-06-01

## Goal: Hermes Security Intelligence 정기 협업 전송 자동화

- 목표: security_intelligence_daily 수집 루프를 안전하게 운영 모드로 정착시켜 매일 `cybersecurity-agent`에 intake 생성 결과를 HITL 검토용 요청으로 전달한다.
- 범위: `security_intelligence_daily.py`, `~/.hermes/.env` 인증/endpoint 설정, OS cron 실행 래퍼 스크립트, 실행 증적 기록.

## 완료기준

- `/home/ktl/.hermes/.env`에 아래 항목이 존재한다.
  - `COLLABORATION_WRITER_API_TOKEN` (write 권한 토큰)
  - `COLLABORATION_API_BASE`
- 다음 실행 래퍼가 존재한다.
  - `/home/ktl/projects/hermes-agent/scripts/run_security_intelligence_daily.sh`
  - `.hermes/.env` 로드 + 가상환경(`venv`) 활성화 + `python3 scripts/security_intelligence_daily.py` 실행
  - 로그를 `/home/ktl/projects/hermes-agent/data/logs/security-intelligence-daily.log`에 기록
- cron 등록이 완료된다.
  - `0 2 * * * /home/ktl/projects/hermes-agent/scripts/run_security_intelligence_daily.sh`
- `security_intelligence_daily.py` 실행이 dry-run 및 live에서 각각 성공적으로 종료한다(기록 기반 확인).
- live 실행에서 `request` 생성 응답이 `success=true`로 남거나, 실패 시 stderr/로그에 원인(예: 인증 오류, API 반환코드)이 남아 추적 가능해야 한다.

## 현재 상태

- `.hermes/.env` 반영 완료
- 래퍼 스크립트 추가 완료
- crontab 등록 완료
- `SEC_INTELLIGENCE_DAILY_LIMIT=5`로 직접 실행하여 요청 생성 성공 로그 확인 완료
