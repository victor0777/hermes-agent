# 2026-05-28

## Security news digest tool

- 목표: qwen36-35b OpenAI-compatible 및 tool call 검증 결과를 바탕으로, Hermes가 web/news 보안 advisory를 수집해 로컬 digest 문서를 만드는 read/write-only 도구를 추가한다.
- 시도: `tools/security_digest_tool.py`를 추가해 `collect_security_intelligence`를 재사용하고, `model_tools.py` discovery 및 `toolsets.py` collaboration toolset에 `security_news_digest`를 연결했다.
- 결과: digest는 기본 CISA advisory RSS(`https://www.cisa.gov/cybersecurity-advisories/all.xml`) 또는 사용자가 넘긴 RSS/JSON url/path source를 읽고, `HERMES_HOME/digests/security-news/YYYY-MM-DD.md`에 Markdown 초안을 작성한다. 외부 publish, collaboration write, operational action은 반환 JSON에 approval-required로 명시했다.
- 검증: `python -m pytest tests/tools/test_security_digest_tool.py -q` 통과(4 passed), `python -m pytest tests/test_model_tools.py tests/test_toolsets.py -q` 통과(32 passed).
- 다음: 필요하면 isolated Hermes HOME + `qwen36-35b` router config로 실제 Hermes CLI smoke test를 수행한다.
